/**
 * Beat 9 — Grid (STA-494). Camera pulls back to reveal all three workflows in
 * their own framed cards, side by side, with the product tagline beneath.
 *
 * CP1 STUB: renders the settled GridScreen. CP3 adds the pull-back + card
 * stagger + tagline reveal.
 */
import { GridScreen } from "../nick-launch-video/screens";

export const WorkflowGridSequence: React.FC = () => <GridScreen />;
