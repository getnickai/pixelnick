/**
 * Beat 10 — Product shell (STA-494). Match-cut: the hero workflow docks into
 * the right builder pane while the chat rail wipes in from the left and the
 * widget cards stream into the thread (SpaceX + NVDA price cards, then the
 * portfolio card), mimicking the real product experience.
 *
 * CP1 STUB: renders the settled ProductScreen. CP4 adds the match-cut wipe +
 * card stream-in.
 */
import { ProductScreen } from "../nick-launch-video/screens";

export const ProductShellSequence: React.FC = () => <ProductScreen />;
